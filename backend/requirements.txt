fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.0.0
numpy
pandas
pillow
requests
scikit-learn
tqdm

# Inference (Keras .h5 — primary)
tensorflow

# Inference (ONNX — offline-only fallback for legacy artifacts)
onnx
onnxruntime
